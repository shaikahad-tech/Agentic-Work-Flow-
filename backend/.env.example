# Copy this file to .env and fill in your API keys.
# Alternatively, you can enter keys in the UI settings panel — they are sent
# with each request and never persisted on the backend.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AIza...
